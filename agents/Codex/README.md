# Codex Skill Mirror

This tree is generated from `kb/skills` (top-level `*.md` and directory packages such as `greenergyfl-finance/`) by `scripts/sync-dome-skills.py`.
It contains agent-specific `SKILL.md` mirrors for Codex-facing workflows.
