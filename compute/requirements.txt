# Claude compute environment — DOME-HUB
# Pinned to known-good versions for Python 3.11.

# --- Local inference (sovereign, air-gapped — preferred over any cloud API) ---
# Apple MLX: native M-series silicon inference, fastest local path
mlx>=0.16.0; sys_platform == "darwin"
mlx-lm>=0.16.0; sys_platform == "darwin"
# Ollama Python client (HTTP interface to local Ollama daemon)
ollama>=0.2.0

# --- Cloud LLM SDKs (secondary — use only when local is insufficient) ---
# Anthropic Claude (preferred cloud provider — no OpenAI dependency)
anthropic>=0.39.0,<1.0.0
claude-agent-sdk>=0.1.0; python_version >= "3.11"
# OpenAI SDK kept for Ollama OpenAI-compat endpoint only — avoid direct OpenAI API use
openai>=1.30.0

# --- Document creation (backs the docx/pdf/pptx/xlsx skills) ---
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
reportlab>=4.0.7
pypdf>=4.2.0
pdfplumber>=0.11.0

# --- Web research ---
requests>=2.31.0
beautifulsoup4>=4.12.0
httpx>=0.27.0

# --- Retrieval / embeddings (for kb/ retrieval-augmented workflows) ---
chromadb>=0.5.0
sentence-transformers>=2.7.0
tiktoken>=0.7.0

# --- Data / analysis ---
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
sympy>=1.12

# --- Quantum computing (lab-grade stack) ---
# IBM — circuit design, simulation, hardware access
qiskit>=1.1.0
qiskit-aer>=0.14.0
# Differentiable QML — hybrid quantum/classical ML (MPS-compatible)
pennylane>=0.36.0
pennylane-qiskit>=0.36.0
# Google-style quantum circuits
cirq-core>=1.3.0
# Open quantum systems, density matrices, Lindblad dynamics
qutip>=4.7.0
# Rigetti-style qubit programming
pyquil>=4.0.0
# AWS multi-backend (IonQ, Rigetti, simulators)
amazon-braket-sdk>=1.83.0
# Photonic / continuous-variable quantum computing
strawberryfields>=0.23.0
# PyTorch with MPS backend (classical side of hybrid VQE / QML loops)
torch>=2.3.0

# --- Dev / tooling ---
python-dotenv>=1.0.1
pydantic>=2.6.0
rich>=13.7.0
typer>=0.12.0
