# config — Export Policy

Controls what gets exported from private DOME-HUB to public DSH.

- `public-export.allowlist` — repo-relative prefixes to include
- `public-export.denylist` — shell globs to exclude
